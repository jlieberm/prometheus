################################################################################
# Package: prometheus
################################################################################

if ($ENV{Athena_SETUP} STREQUAL "on")
# Declare the package name:
atlas_subdir( Validation )
# Install files from the package:

endif()

